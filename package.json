{
  "name": "efi-pix-vercel",
  "version": "1.0.0",
  "description": "Vercel serverless function for EFI Bank PIX with mTLS",
  "main": "api/process-pix.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}